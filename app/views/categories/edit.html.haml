- @title = "Edit Category: #{@category.name_was}"

- content_for :body_class do
  brand-background

- content_for :header do
  .header-container
    .container
      %h1.page-heading
        = @category.name_was
  .breadcrumb-container{ style: 'margin-bottom: 0;' }
    .container
      %ol
        %li= link_to 'Categories', categories_path
        %li= link_to @category.name_was, @category
        %li Edit

.dashboard-container
  = render 'form'
