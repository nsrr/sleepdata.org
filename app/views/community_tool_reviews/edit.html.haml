- @title = "Update Review - #{@community_tool.name}"

- content_for :header do
  .header-container
    .container
      %h1.page-heading
        Update Review
  .breadcrumb-container
    .container
      %ol
        %li= link_to @community_tool.name, tool_path(@community_tool)

.dashboard-container
  = render "form", community_tool_review: @community_tool_review
