$("#agreement_events").html("<%=j render "agreement_events/index" %>");

<%# Reset form %>
<% @agreement_event = @data_request.agreement_events.where(user_id: current_user.id).new %>
$("#agreement_events_new_form").html("<%=j render "agreement_events/form" %>");

datepickerReady();
