= form_with model: image, local: true do |form|
  = render "forms/horizontal/file_field", form: form, object: image, key: :image, help_text: "JPG, GIF, PNG"
  = render "forms/horizontal/submit", form: form, object: image
