- @title = "Edit Topic: #{@topic.title_was}"

- content_for :header do
  .header-container
    .container
      %h1.page-heading
        = @topic.title_was
  .breadcrumb-container
    .container
      %ol
        %li= link_to @topic.title_was, @topic
        %li Edit

= render "form"
