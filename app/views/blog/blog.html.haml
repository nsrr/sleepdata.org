- @title = 'Blog'

.row
  .col-sm-9
    - @broadcasts.each do |broadcast|
      %h2.broadcast-title= link_to broadcast.title, blog_post_path(broadcast)
      = render 'blog/show', broadcast: broadcast
    .center= paginate @broadcasts, theme: 'contour'
  .col-sm-3
    %h2.brand NSRR Blog
