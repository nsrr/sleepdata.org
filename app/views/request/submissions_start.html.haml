- content_for :header do
  .header-container
    .container
      %h1.page-heading.text-center
        Looking to
        %i.fa.fa-cloud-download
        download data?

.jumbotron
  %p.text-center
    Proceed through
    %strong
      a few short steps
    of the submission review process.
  %p.text-center
    Save your progress whenever, "Submit your Application" when ready!
  %p.text-center
    %br
    = link_to submissions_launch_path(dataset: params[:dataset]), method: :post, class: "btn btn-lg btn-primary btn-shadow" do
      Click to start!
      %i.fa.fa-caret-right

  %div{ style: "margin-top: 80px;" }

    %p.text-center
      We're here to help! Send your questions to
      = succeed "." do
        = mail_to ENV["support_email"]
