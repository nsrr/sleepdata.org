<div class="page-header">
  <h1>Install GNU Wget for Windows</h1>
  <%= link_to 'Looking for a different operating system?', wget_path(choose: '1') %>
</div>

<div class="bs-callout bs-callout-danger">
  <h4>Important</h4>

  <p>
    Requires Administrator Privileges!
  </p>
</div>

<% wget_file = 'wget-1.11.4-1-setup.exe' %>

<ol>
<li>
  Download <%= link_to wget_file, "http://downloads.sourceforge.net/gnuwin32/#{wget_file}" %>.
</li>
<li>
  Double click <code><%= wget_file %></code> and follow installation instructions.
</li>
<li>
  Click the Windows Start Button.
</li>
<li>
  Click the <b>search bar</b>, type <code>Command Prompt</code>, right click <code>Command Prompt</code>, and select <b>Run as Administrator</b>.
</li>
<li>
  Add the installation directory to the user PATH environment variable.
  <pre class='code'>setx PATH "C:\Program Files (x86)\GnuWin32\bin"</pre>
</li>
<li>
  Verify GNU Wget installation in the Command Prompt.
  <pre class='code'>wget --version

  <span class="text-muted">GNU Wget <%= wget_file.split('-setup.exe').first.split('wget-').last %></span></pre>
</li>
</ol>

