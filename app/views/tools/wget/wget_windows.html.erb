<div class="page-header">
  <h1>Install GNU Wget for Windows</h1>
  <%= link_to 'Looking for a different operating system?', wget_path(choose: '1') %>
</div>

<div class="bs-callout bs-callout-danger">
  <h4>Important</h4>

  <p>
    Requires Administrator Privileges!
  </p>
</div>


<% wget_file = 'wget-1.11.4-1-bin.zip' %>

<ol>
<li>
  Download <%= link_to wget_file, "http://downloads.sourceforge.net/gnuwin32/#{wget_file}" %>.
</li>
<li>
  Extract <code><%= wget_file %></code>.
</li>
<li>
  Copy <code>wget.exe</code> in <code><%= wget_file %>\bin\</code> to<code>C:\Windows\</code>
</li>
<li>
  Click the Windows Start Button.
</li>
<li>
  Click the <b>search bar</b> or <b>Run...</b> and type <code>command</code> and click <code>Enter</code>.
</li>
<li>
  Verify GNU Wget installation in the Command Prompt.
  <pre class='code'>wget --version

  <span class="text-muted">GNU Wget <%= wget_file.split('wget-').last.split('-').first %></span></pre>
</li>
</ol>

