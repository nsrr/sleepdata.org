- @title = "Edit User: #{@user.username_was}"
- render "themes/theme"

- content_for :container_class, "container-fluid"

- content_for :sidebar, render("admin/sidebar")

- content_for :header do
  = link_to @user.username_was, @user
  Edit

.dashboard-container
  = render "form", user: @user
