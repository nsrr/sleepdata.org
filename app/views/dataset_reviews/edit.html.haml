- @title = "Update Review - #{@dataset.name}"

- content_for :header do
  .header-container
    .container
      %h1.page-heading
        Update Review
  .breadcrumb-container
    .container
      %ol
        %li= link_to @dataset.name, @dataset

.dashboard-container
  = render "form", dataset_review: @dataset_review
