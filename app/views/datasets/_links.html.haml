- if current_user && @dataset.editable_by?(current_user)
  .center{ style: 'margin-top:20px' }
    = link_to 'Audits', audits_dataset_path(@dataset), class: 'btn btn-xs btn-default'
    = link_to 'Collaborators', collaborators_dataset_path(@dataset), class: 'btn btn-xs btn-default'
