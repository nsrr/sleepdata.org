- if export.zipped_file.present?
  = link_to [:download, export.organization, export], data: { turbolinks: false } do
    = icon("far", "file-archive")
    = export.name
- else
  .text-muted Processing...
