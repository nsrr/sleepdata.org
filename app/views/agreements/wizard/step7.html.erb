<%= render partial: 'agreements/wizard/irb_progress' %>

<h2 style="text-align:center">EVIDENCE OF IRB REVIEW</h2>

<hr class="divider-md" />

<%= form_for @agreement, url: update_step_agreement_path(@agreement), method: :patch, multipart: true, html: { class: 'form-vertical', id: 'update_step_form' } do |f| %>
  <% if @agreement.errors.any? %>
    <div id="error_explanation" class="bs-callout bs-callout-danger">
      <h4><%= pluralize(@agreement.errors.count, "error") %> prohibited this step from being saved</h4>

      <ul>
      <% @agreement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= hidden_field_tag "agreement[draft_mode]", nil %>
  <%= hidden_field_tag "step", @step %>
  <%= hidden_field_tag "agreement[current_step]", @step %>

  <p class="lead-paragraph">
    In addition to completing and executing the NSRR Data Access and Use Agreement for the research project/specific purpose identified in your request, you will also need to submit evidence of  Institutional Review Board (IRB)/Ethics Committee review of your project to ensure the data will be used for appropriate research purposes and to ensure data security and appropriate handling of the data at the users site. This IRB review can be obtained locally and submitted with the DAUA, or by completion of the online request form if you would like to apply for NSRR IRB review of your project.
  </p>

  <div class="row">
    <div class="col-sm-6">
      <div class="panel panel-info panel-hover" data-object="select_radio_button" data-target="#agreement_irb_evidence_type_has_evidence">
        <div class="panel-heading">
          Option 1 (Faster)
        </div>
        <div class="panel-body">

          <div class="form-group">
            <div class="radio">
              <label>
                <%= f.radio_button :irb_evidence_type, 'has_evidence' %>
                Please attach evidence of IRB review. It is expected the document attached shows the IRB has either a) performed a review of the project and issued an approval, or b) declared that the research project is exempt from review.
              </label>
            </div>
          </div>

          <div class="form-group">
            <% if @agreement.irb.size > 0 %>
              <div class="bs-callout bs-callout-default">
                <%= link_to "<span class='glyphicon glyphicon-file'></span> #{@agreement.irb.to_s.split('/').last}".html_safe, download_irb_agreement_path(@agreement), target: '_blank' %>
                <tt><%= number_to_human_size(@agreement.irb.size) %></tt>
              </div>
            <% end %>

            <%= f.label :irb, 'Evidence of IRB review (PDF)', class: 'control-label' %>


            <%= f.file_field :irb, class: 'form-control' %>
            <%#= f.hidden_field :irb_cache %>
            <div class="bs-callout bs-callout-info">
              <h4>IRB Template</h4>

              <p><%= link_to "<span class='glyphicon glyphicon-link'></span> IRB Assistance Template for Evidence of IRB Review".html_safe, irb_assistance_template_path, target: '_blank' %></p>
            </div>
          </div>

        </div>
      </div>
    </div>
    <div class="col-sm-6">
      <div class="panel panel-info panel-hover" data-object="select_radio_button" data-target="#agreement_irb_evidence_type_no_evidence">
        <div class="panel-heading">
          Option 2 (Slower)
        </div>
        <div class="panel-body">
          <div class="form-group">
            <div class="radio">
              <label>
                <%= f.radio_button :irb_evidence_type, 'no_evidence' %>
                I don't have IRB Review, and would like the NSRR Internal Review Committee to conduct the review. <strong>Please allow 3 to 4 weeks for review.</strong>
                Select this to fill out the online request form if you would like to apply for NSRR Internal Review Committee review of your project.
              </label>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <hr class="divider-md" />

  <div class="row">
    <div class="col-xs-4">
      <%= link_to "<span class='glyphicon glyphicon-arrow-left'></span> Back to Previous Step".html_safe, step_agreement_path(@agreement, step: @step-1), class: 'btn btn-lg btn-default btn-block' %>
    </div>
    <div class="col-xs-4">
      <%= link_to "<span class='glyphicon glyphicon-floppy-disk'></span> Save Draft".html_safe, '#', data: { object: 'submit-draft', target: '#update_step_form' }, class: 'btn btn-lg btn-warning btn-block' %>
    </div>
    <div class="col-xs-4">
      <%= link_to "<span class='glyphicon glyphicon-floppy-disk'></span> Save and Continue".html_safe, '#', data: { object: 'submit', target: '#update_step_form' }, class: 'btn btn-lg btn-primary btn-block' %>
    </div>
  </div>
<% end %>

<% @footer_img = 'footer.jpg' %>
