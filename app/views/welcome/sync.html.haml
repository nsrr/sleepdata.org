- @title = 'Sync Status'
.row
  .col-md-3
    = render 'internal/profile'
    .dashboard-link
      = link_to admin_path do
        %span.glyphicon.glyphicon-arrow-left
        Back to Admin Dashboard

    .dashboard-container
      .dashboard-title
        %strong= @title
      %p All your admin needs.


  .col-md-9
    .dashboard-container
      .dashboard-title
        %strong= @title

      - Dataset.current.order(:name).each do |dataset|
        = render 'documentation/sync', local_commit: dataset.local_commit, remote_commit: dataset.remote_commit, object: dataset, back: 'sync'

      - Tool.current.order(:name).each do |tool|
        = render 'documentation/sync', local_commit: tool.local_commit, remote_commit: tool.remote_commit, object: tool, back: 'sync'
