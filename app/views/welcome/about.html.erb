<div class="page-header">
  <h1>About the National Sleep Research Resource</h1>
</div>

<% first_half = @users.collect.with_index{|u, i| [u, i]}.select{|u, i| i < (@users.size + 1) / 2 }.collect{|u, i| u} %>
<% second_half = @users.collect.with_index{|u, i| [u, i]}.select{|u, i| i >= (@users.size + 1) / 2 }.collect{|u, i| u} %>

<div class="row">
  <div class="col-xs-12 col-sm-6">
    <% first_half.each do |user| %>
      <%= render partial: 'user_panel', locals: { user: user } %>
    <% end %>
  </div>

  <div class="col-xs-12 col-sm-6">
    <% second_half.each do |user| %>
      <%= render partial: 'user_panel', locals: { user: user } %>
    <% end %>
  </div>
</div>

<div class="welcome-box welcome-even">
  <div class="jumbotron" style="padding:20px 0px 0px 0px">
    <div class="row">
      <div class="col-xs-12 col-sm-4"><h4 style="text-align:center">Beth Israel Deaconess Medical Center</h4></div>
      <div class="col-xs-12 col-sm-4"><h4 style="text-align:center">Brigham and Women's Hospital</h4></div>
      <div class="col-xs-12 col-sm-4"><h4 style="text-align:center">Case Western Reserve University</h4></div>
    </div>
  </div>
</div>

<div class="center">
  <p class="lead">
    The National Sleep Research Resource (NSRR) is funded by the NIH National Heart, Lung, and Blood Institute.
  </p>
  <p class="lead">
    An interdisciplinary group, including sleep physicians, informaticians, mathematicians, and other research staff are working to build the NSRR, alongside a highly talented <%= link_to 'Academic User Group', aug_path %>.
  </p>
</div>

<div class="center" style="margin-top:60px">
  <%= image_tag 'nsrr_logo_256.png', size: '256x256', alt: 'NSRR' %>
</div>

<%= render partial: 'layouts/footer' %>
