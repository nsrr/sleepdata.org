Dear <%= @user.username %>,

The following data requests are awaiting your review. Please click on each and then approve or reject the data request.

<% if @user.digest_reviews.present? %>
Data Requests

<% @user.digest_reviews.each_with_index do |review, index| %>
----------------------------

<%= review.agreement.name %> - <%= "#{ENV["website_url"]}/reviews/#{review.agreement.to_param}" %>

<% end %>
<% end %>

<%= "#{ENV["website_url"]}/reviews" %>

<%= @footer_txt %>
