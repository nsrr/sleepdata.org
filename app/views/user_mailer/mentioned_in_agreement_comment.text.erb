Dear <%= @user.first_name %>,

<%= @agreement_event.user.name %> mentioned you while reviewing an agreement.

<%= "#{@agreement_event.agreement.name_with_id} #{SITE_URL}/reviews/#{@agreement_event.agreement.to_param}" %>

  <%= render partial: 'digest_review_comment' %>


Click the following link to view the comment:

<%= "#{SITE_URL}/reviews/#{@agreement_event.agreement.to_param}#c#{@agreement_event.number}" %>
