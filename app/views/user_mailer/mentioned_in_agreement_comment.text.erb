Dear <%= @user.username %>,

<%= @agreement_event.user.username %> mentioned you while reviewing a data request.

<%= "#{@agreement_event.agreement.name} #{ENV["website_url"]}/reviews/#{@agreement_event.agreement.to_param}" %>

  <%= render "digest_review_comment" %>


Click the following link to view the comment:

<%= "#{ENV["website_url"]}/reviews/#{@agreement_event.agreement.to_param}#c#{@agreement_event.number}" %>
