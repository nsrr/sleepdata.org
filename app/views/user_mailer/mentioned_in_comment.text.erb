Dear <%= @user.first_name %>,

<%= @comment.user.forum_name %> mentioned you in a comment on the forum.

<%= "#{@comment.topic.name} #{ENV['website_url']}/forum/#{@comment.topic.to_param}" %>

  <%= render partial: 'digest_comment' %>


Click the following link to view the comment:

<%= "#{ENV['website_url']}/forum/#{@comment.topic.to_param}?page=#{((@comment.number - 1) / Comment::COMMENTS_PER_PAGE)+1}#c#{@comment.number}" %>


You may turn off these email notifications in your settings: <%= "#{ENV['website_url']}/settings" %>
