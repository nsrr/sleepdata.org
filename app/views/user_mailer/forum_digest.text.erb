Dear <%= @user.first_name %>,

<% if @user.digest_comments.size > 0 %>
Forum Updates

<% @user.digest_comments.group_by{|s| s.topic_id}.each do |topic_id, comments| %>
----------------------------

<%= ((topic = Topic.find_by_id(topic_id)) ? "#{topic.name} #{SITE_URL}/forum/#{topic.to_param}" : 'No Topic') %>

  <% comments.sort_by(&:id).each do |comment| %>
  <%= @comment = comment; render partial: 'digest_comment' %>
  <% end %>

<% end %>
<% end %>


<%= @footer_txt %>
