# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  data_request: uploads
  document: blank.pdf
  file_size: 10892

# This one gets deleted, and isn't stored in git
two:
  data_request: uploads
  document: rails.png
  # file_size: 0

# This one gets deleted, and isn't stored in git
reviewer_uploaded:
  data_request: uploads
  document: rails.png
  # file_size: 0
  reviewer_uploaded: true
